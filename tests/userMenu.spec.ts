import {test} from '@pages/fixtures/testBase'

test.describe('tests of links redirection from user menu', () => {

test('Go to All Items', async ({ cartPage, dashboardPage }) => {
});

test('Go to About', async ({ loginPage }) => {
});

test('Go to Reset App State', async ({ loginPage }) => {
});

})
