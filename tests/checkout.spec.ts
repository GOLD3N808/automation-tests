import {test} from '@pages/fixtures/testBase'

test.describe('tests of buying products from cart, and finalize checkout', () => {

test('Buy products - happy path', async ({ page }) => {

});

test('Buy products - unhappy path invalid zip code', async ({ page }) => {

});

test('Buy products - unhappy path - empty customer information fields', async ({ page }) => {
    
});

test('Buy products - back to change client information', async ({ page }) => {
    
});

test('Buy products - back to cart', async ({ page }) => {
    
});

test('Checkout with empty cart', async ({ page }) => {
    
});

})